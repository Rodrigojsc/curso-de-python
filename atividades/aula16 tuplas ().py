#lanche = ('Hamburguer', 'Suco', 'Pizza', 'Pudim')
#print(lanche[2:]) #'(lanche[2:0]) irá pegar as palavras da segunda em diante
# {sorted(lanches)} irá colocar tudo em ordem alfabética
# {lanche.index()} vai encontrar a posicao em que está na tupla, exemplo: lanche = ('Hamburguer', 'Suco', 'Pizza', 'Pudim') Suco está na primeira posicao (começa do 0)

#lanche  = ('Hamburguer', 'Suco', 'Pizza', 'Pudim')

#for cont in range(0,len(lanche)):
#    print(f'Eu comi {lanche[cont]}')    #primeira maneira de usar tupla

#for comida in lanche:
#    print(f'Eu vou comer {comida}.')    #segunda maneira de usar tupla

#for pos, comida in enumerate(lanche):
#    print(f'Eu vou comer {comida} na posição {pos}.')   #tupla contabilizando o lugar de cada um

#print('Comi demais')

pessoa = ('Rodrigo', 19, 'M', 62.5)
#del (pessoa) #tambem é possivel apagar uma tupla ou variavel.
print(pessoa[1])
